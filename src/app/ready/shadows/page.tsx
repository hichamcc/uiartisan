import Layout from '@/components/Layout';
import ShadowPage from '@/components/ready/shadow';

export default function LayoutGeneratorPage() {
    return (
        <Layout>
            <ShadowPage />
        </Layout>
    );
}