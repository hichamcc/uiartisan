import Layout from '@/components/Layout';
import ButtonsPage from '@/components/ready/buttons';

export default function LayoutGeneratorPage() {
    return (
        <Layout>
            <ButtonsPage />
        </Layout>
    );
}