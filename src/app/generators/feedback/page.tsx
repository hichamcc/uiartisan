import FeedbackCardGenerator from '@/components/generators/FeedbackGenerator';
import Layout from '@/components/Layout';

export default function LayoutGeneratorPage() {
    return (
        <Layout>
            <FeedbackCardGenerator />
        </Layout>
    );
}