import AlertNotificationGenerator from '@/components/generators/AlertGenerator';
import Layout from '@/components/Layout';

export default function LayoutGeneratorPage() {
    return (
        <Layout>
            <AlertNotificationGenerator />
        </Layout>
    );
}