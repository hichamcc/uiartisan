import DivPositionGenerator from '@/components/generators/DivPositionGenerator';
import Layout from '@/components/Layout';

export default function LayoutGeneratorPage() {
    return (
        <Layout>
            <DivPositionGenerator />
        </Layout>
    );
}