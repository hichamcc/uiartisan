import TagInputGenerator from '@/components/generators/TagGenerator';
import Layout from '@/components/Layout';

export default function LayoutGeneratorPage() {
    return (
        <Layout>
            <TagInputGenerator />
        </Layout>
    );
}