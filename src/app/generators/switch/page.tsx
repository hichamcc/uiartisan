import SwitchGenerator from '@/components/generators/SwitchGenertor';
import Layout from '@/components/Layout';

export default function LayoutGeneratorPage() {
    return (
        <Layout>
            <SwitchGenerator />
        </Layout>
    );
}