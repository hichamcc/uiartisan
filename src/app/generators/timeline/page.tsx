import TimelineGenerator from '@/components/generators/TimelineGenerator';
import Layout from '@/components/Layout';

export default function LayoutGeneratorPage() {
    return (
        <Layout>
            <TimelineGenerator />
        </Layout>
    );
}