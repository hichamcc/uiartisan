import Layout from '@/components/Layout';
import FAQGenerator from '@/components/generators/FaqGenerator';

export default function InputGeneratorPage() {
    return (
        <Layout>
            <FAQGenerator />
        </Layout>
    );
}