import GSAPGenerator from '@/components/generators/GSAPgenerator';
import Layout from '@/components/Layout';

export default function LayoutGeneratorPage() {
    return (
        <Layout>
            <GSAPGenerator />
        </Layout>
    );
}