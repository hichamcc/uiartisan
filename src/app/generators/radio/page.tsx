import RadioGenerator from '@/components/generators/Radiogenerator';
import Layout from '@/components/Layout';

export default function LayoutGeneratorPage() {
    return (
        <Layout>
            <RadioGenerator />
        </Layout>
    );
}