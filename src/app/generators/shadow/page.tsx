import ShadowDivGenerator from '@/components/generators/ShadowGenerator';
import Layout from '@/components/Layout';

export default function LayoutGeneratorPage() {
    return (
        <Layout>
            <ShadowDivGenerator />
        </Layout>
    );
}