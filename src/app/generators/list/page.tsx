import ListGenerator from '@/components/generators/ListGenerator';
import Layout from '@/components/Layout';

export default function LayoutGeneratorPage() {
    return (
        <Layout>
            <ListGenerator />
        </Layout>
    );
}