import Layout from '@/components/Layout';
import CheckboxGenerator from '@/components/generators/CheckboxGenerator';

export default function InputGeneratorPage() {
    return (
        <Layout>
            <CheckboxGenerator />
        </Layout>
    );
}