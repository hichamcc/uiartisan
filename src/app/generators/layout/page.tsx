import LayoutGenerator from '@/components/generators/LayoutGenerator';
import Layout from '@/components/Layout';

export default function LayoutGeneratorPage() {
    return (
        <Layout>
            <LayoutGenerator />
        </Layout>
    );
}