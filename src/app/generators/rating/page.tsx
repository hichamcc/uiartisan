import RatingGenerator from '@/components/generators/RatingGenerator';
import Layout from '@/components/Layout';

export default function LayoutGeneratorPage() {
    return (
        <Layout>
            <RatingGenerator />
        </Layout>
    );
}