"use client";
import OverenthusiasticSubmitButton from '@/components/funny/Submit';
import Layout from '@/components/Layout';

export default function LayoutGeneratorPage() {
    return (
        <Layout>
            <OverenthusiasticSubmitButton />
        </Layout>
    );
}